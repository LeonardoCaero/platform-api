meta {
  name: List Projects
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/projects?companyId={{companyId}}&isActive=true
  body: none
  auth: bearer
}

params:query {
  companyId: {{companyId}}
  isActive: true
  ~search: 
}

auth:bearer {
  token: {{accessToken}}
}
