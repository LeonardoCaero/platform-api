meta {
  name: List Clients
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/clients?companyId={{companyId}}&isActive=true&page=1&limit=20
  body: none
  auth: bearer
}

params:query {
  companyId: {{companyId}}
  isActive: true
  page: 1
  limit: 20
  ~search: 
}

auth:bearer {
  token: {{accessToken}}
}
