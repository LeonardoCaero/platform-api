meta {
  name: Update Rate Rule
  type: http
  seq: 2
}

patch {
  url: {{baseUrl}}/clients/rates/:ruleId
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "Updated Rate",
    "baseRatePerHour": 55,
    "overtimeRatePerHour": 80,
    "currency": "EUR",
    "isActive": true
  }
}
