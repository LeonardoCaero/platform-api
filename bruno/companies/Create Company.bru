meta {
  name: Create Company
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/companies
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "name": "My Company",
    "slug": "my-company",
    "description": "Company description"
  }
}
