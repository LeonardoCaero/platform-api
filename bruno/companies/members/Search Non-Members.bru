meta {
  name: Search Non-Members
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/companies/{{companyId}}/non-members
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  ~search: john
}
