meta {
  name: Upload Logo
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/uploads/logo
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:multipartForm {
  file: @file(/path/to/logo.png)
}
