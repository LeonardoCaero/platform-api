meta {
  name: List Categories
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/time-entry-categories
  body: none
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

params:query {
  companyId: {{companyId}}
  ~isActive: true
}
