meta {
  name: Update Request
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/permission-requests/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "reason": "Updated reason for the request"
  }
}
