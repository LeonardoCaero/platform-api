meta {
  name: Update Permission
  type: http
  seq: 4
}

patch {
  url: {{baseUrl}}/permissions/:id
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "key": "PERMISSION_KEY",
    "description": "Updated description for this permission"
  }
}
